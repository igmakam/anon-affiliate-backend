services:
- type: web
  name: anon-affiliate-backend
  env: python
  plan: starter
  buildCommand: pip install -r requirements.txt
  startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
  envVars:
  - key: PUBLIC_APP_BASE
    sync: false
  - key: DATABASE_URL
    value: sqlite:///./app.db
